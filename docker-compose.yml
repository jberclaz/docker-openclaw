services:
  openclaw:
    build: .
    init: true
    stop_grace_period: 30s
    ports:
      - "18789:18789"
    volumes:
      - openclaw_data:/home/node
    stdin_open: true
    tty: true

volumes:
  openclaw_data:
    name: openclaw_data
