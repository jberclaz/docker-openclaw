services:
  openclaw:
    image: ghcr.io/jberclaz/docker-openclaw:latest
    ports:
      - "18789:18789"
    volumes:
      - openclaw_data:/home/ubuntu
    stdin_open: true
    tty: true

volumes:
  openclaw_data:
    name: openclaw_data
